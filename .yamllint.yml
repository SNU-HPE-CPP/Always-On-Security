extends: default

rules:
  line-length:
    max: 120
  truthy: disable
  document-start: disable
  comments:
    min-spaces-from-content: 1
  key-duplicates:
    level: error
  indentation:
    spaces: 2
    indent-sequences: true
    check-multi-line-strings: false
