# Baseline SHA-256 hashes of critical configuration files.
# Generated by: python3 generate_baseline.py
#
# IMPORTANT: Regenerate this file from a known-good node state before deploying.
# Any deviation from these hashes triggers a CONFIG_TAMPER alert.
# If a file is absent from a node (e.g., container lacks /etc/sudoers),
# it is silently skipped — no false positive.
#
# Files with no hash entry are NOT monitored.
# Add entries here to extend monitoring to additional files.

# Placeholder hashes — run generate_baseline.py to populate with real values.
# Format: "<absolute_path>": "<sha256_hex_digest>"
/etc/hosts: ""
/etc/passwd: ""
